<!DOCTYPE html>
<html>
<head>
  <title>mooculus</title>
    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= favicon_link_tag '/favicon.ico' %>
    <%= csrf_meta_tags %>
    <link href='http://fonts.googleapis.com/css?family=Open+Sans:300italic,400italic,600italic,700italic,800italic,400,600,300,800,700' rel='stylesheet' type='text/css'>
</head>
<body class="<%= @splash ? 'splash' : ''%>">
<%= bootstrap_flash %>

  <div class="navbar navbar-inverse navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container">
        <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </a>
        <a class="brand" href="/"><%=raw html_logo %></a>
        <div class="nav-collapse">
          <ul class="nav">
            <li class="<%= 'active' if params[:controller] == 'videos' %>"><%= link_to raw("<i class='icon-film'></i> Lectures"), :controller => "videos", :action => "index" %></li>
            <li class="<%= 'active' if params[:controller] == 'handouts' %>"><%= link_to raw("<i class='icon-book'></i> Textbook"), :controller => "handouts", :action => "index" %></li>
            <li class="<%= 'active' if params[:controller] == 'exercises' && params[:action] != "progress" %>"><%= link_to raw("<i class='icon-pencil'></i> Exercises"), :controller => "exercises", :action => "index" %></li>
            <% if user_signed_in? %>
              <li><a href="https://class.coursera.org/calc1-001/forum/index"><i class="icon-comments"></i> Forum</a></li>
            <% end %>
	  </ul>
	  <ul class="nav pull-right">
            <% if user_signed_in? %>
              <li class="dropdown-right">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                  <i class="icon-user"></i> <%= current_user.name %>
                  <b class="caret"></b>
                </a>
                <ul class="dropdown-menu">
                  <li><%= link_to raw("<i class='icon-tasks'></i> View progress"), :controller => "exercises", :action => "progress" %></li>
                  <li><%= link_to raw("<i class='icon-signout'></i> Log out"), destroy_user_session_path, :method => :delete %></li>
                </ul>
              </li>
            <% else %>
              <li class=""><%= link_to raw("<i class='icon-signin'></i> Login"), user_omniauth_authorize_path(:coursera) %></li>
            <% end %>
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </div>
  </div>

  <div class="container-fluid">
    <%= yield %>

    <hr>

    <footer>
      <p>&copy; <%= Date.today.strftime( '%Y' ) %>, <a href="/developers"><%=raw html_logo %> team</a>.
    </footer>
  </div> <!-- /container -->

  <%= piwik_tracking_tag %>
  </body>
</html>
